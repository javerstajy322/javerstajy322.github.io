# В папке 'complited' собран пустой шаблон под gulp.
===============
npm -ls --depth=0  // derevo 0 vlojenosti
npm up // podnjat paketi
--save-dev // local istall package

gulp // init project

========
